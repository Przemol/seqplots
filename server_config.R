Sys.setenv('root'='/var/shiny-server/DATA')
Sys.setenv('web' = getwd() )
# Uncomment following line debug mode. Warning - it will cause securyty threat, never run this on public acces.
# Sys.setenv('seqplots_debug' = TRUE )
